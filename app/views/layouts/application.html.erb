<!DOCTYPE html>
<html lang="ka">
  <head>
    <title><%= content_for(:title) || "Bait Mate" %></title>
    <meta name="viewport" content="width=device-width,initial-scale=1,maximum-scale=1,user-scalable=no,viewport-fit=cover">
    <meta name="apple-mobile-web-app-capable" content="yes">
    <meta name="apple-mobile-web-app-status-bar-style" content="black-translucent">
    <meta name="application-name" content="Bait Mate">
    <meta name="mobile-web-app-capable" content="yes">
    <meta name="turbo-cache-control" content="no-preview">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= yield :head %>

    <link rel="icon" href="/icon.png" type="image/png">
    <link rel="icon" href="/icon.svg" type="image/svg+xml">
    <link rel="apple-touch-icon" href="/icon.png">
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=AmericanTypewriter&display=swap" rel="stylesheet">

    <%= stylesheet_link_tag :app, "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
  </head>

  <body class="bg-white min-h-screen <%= 'turbo-native' if turbo_native_app? %>" data-controller="header-scroll">
    <% if turbo_native_app? %>
      <script>
        // Communicate auth state to native iOS app
        (function() {
          if (window.webkit && window.webkit.messageHandlers && window.webkit.messageHandlers.nativeApp) {
            window.webkit.messageHandlers.nativeApp.postMessage({
              type: 'authState',
              authenticated: <%= logged_in? ? 'true' : 'false' %>
            });
          }
        })();
      </script>
    <% end %>

    <!-- Navigation -->
    <%= render "layouts/header_nav" %>

    <!-- Flash Messages -->
    <%= render "layouts/flash" %>

    <!-- Main Content -->
    <main class="pt-16 <%= turbo_native_app? ? 'pb-8' : 'pb-24 md:pb-8' %> max-w-7xl mx-auto">
      <%= yield %>
    </main>

    <!-- Mobile Bottom Navigation (hidden in native app - native tab bar is used) -->
    <% if logged_in? && !turbo_native_app? %>
      <%= render "layouts/mobile_nav" %>
    <% end %>
  </body>
</html>
