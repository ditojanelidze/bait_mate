<%# locals: (title:) %>
<div class="min-h-[calc(100vh-80px)] flex justify-center">
  <div class="w-full max-w-md">
    <%= render "components/card" do %>
      <h1 class="text-2xl font-bold text-center text-gray-800 mb-8">
        <%= title %>
      </h1>
      <%= yield %>
    <% end %>
  </div>
</div>
