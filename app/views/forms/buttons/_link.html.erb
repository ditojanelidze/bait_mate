<%# locals: (url:, label:, full_width: true) %>
<%= link_to url,
    class: "#{full_width ? 'w-full block' : ''} bg-blue-600 text-white text-center py-3 px-4 rounded-lg font-medium hover:bg-blue-700 transition-colors" do %>
  <%= label %>
<% end %>
