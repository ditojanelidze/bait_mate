<div class="max-w-lg mx-auto">
  <div class="bg-white rounded-2xl shadow-xl overflow-hidden">
    <!-- Profile Header -->
    <div class="bg-gradient-to-r from-blue-500 to-blue-600 px-6 py-8 text-center">
      <div class="mb-4 flex justify-center">
        <%= render "components/avatar", user: current_user, size: :lg %>
      </div>
      <h1 class="text-2xl font-bold text-white"><%= current_user.full_name %></h1>
      <p class="text-blue-100"><%= current_user.phone_number %></p>
    </div>

    <!-- Profile Actions -->
    <div class="p-6">
      <%= render "forms/buttons/link", url: edit_profile_path, label: t("profile.edit_title") %>
    </div>
  </div>
</div>
